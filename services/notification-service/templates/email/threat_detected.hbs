<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Threat Detected - {{threat.title}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: {{severityColor threat.severity}}; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9f9f9; }
        .threat-details { background: white; padding: 15px; margin: 15px 0; border-left: 4px solid {{severityColor threat.severity}}; }
        .button { display: inline-block; padding: 12px 24px; background: #007bff; color: white; text-decoration: none; border-radius: 4px; }
        .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚨 {{threat.severity}} Threat Detected</h1>
            <p>{{threat.title}}</p>
        </div>
        
        <div class="content">
            <div class="threat-details">
                <h2>Threat Details</h2>
                <p><strong>ID:</strong> {{threat.id}}</p>
                <p><strong>Category:</strong> {{threat.category}}</p>
                <p><strong>Severity:</strong> {{threat.severity}}</p>
                <p><strong>Description:</strong> {{threat.description}}</p>
                <p><strong>Detected:</strong> {{formatDate threat.detectedAt 'long'}}</p>
            </div>

            {{#if threat.affectedAssets}}
            <div class="threat-details">
                <h3>Affected Assets</h3>
                <ul>
                {{#each threat.affectedAssets}}
                    <li>{{this}}</li>
                {{/each}}
                </ul>
            </div>
            {{/if}}

            {{#if threat.recommendations}}
            <div class="threat-details">
                <h3>Recommended Actions</h3>
                <ol>
                {{#each threat.recommendations}}
                    <li>{{this}}</li>
                {{/each}}
                </ol>
            </div>
            {{/if}}

            <div style="text-align: center; margin: 30px 0;">
                <a href="{{actionUrl}}" class="button">View Threat Details</a>
            </div>
        </div>
        
        <div class="footer">
            <p>This is an automated notification from Threat Modeling Application</p>
            <p>To unsubscribe from email notifications, <a href="{{unsubscribeUrl}}">click here</a></p>
        </div>
    </div>
</body>
</html>